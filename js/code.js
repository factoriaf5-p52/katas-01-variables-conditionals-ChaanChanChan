function evenOrOdd(n) {
    if (n % 2 == 0) {
        return 'even';
    } else {
        return 'odd';
    }
}

function greeting(name) {
    if (name === "Javi") {
        return 'Hola, ' + name + '!';
    } else if (name === "gente") {
        return 'Hola, ' + name + '!';
    } else {
        return 'Hola, ' + name + '!';
    }
}